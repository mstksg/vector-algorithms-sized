name:                vector-algorithms-sized
version:             0.1.0.0
github:              "mstksg/vector-algorithms-sized"
license:             BSD3
author:              "Justin Le"
maintainer:          "justin@jle.im"
copyright:           "(c) Justin Le 2018"

extra-source-files:
- README.md
- CHANGELOG.md

# Metadata used when publishing your package
synopsis:            vector-sized wrapper for vector-algorithms 
category:            Data

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/mstksg/vector-algorithms-sized#readme>

dependencies:
- base >= 4.7 && < 5
- vector-algorithms

library:
  source-dirs: src
